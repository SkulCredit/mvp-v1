# ── HTTPS Server Block (optional) ────────────────────────────────────────────
# Uncomment and configure once you have a TLS certificate.
# For local dev everything runs on port 80 via 05-server-http.conf.
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#     ssl_session_cache   shared:SSL:10m;
#     ssl_session_timeout 10m;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#
#     # Reuse the same location blocks from 05-server-http.conf
#     include /etc/nginx/conf.d/05-server-http.conf;
# }
